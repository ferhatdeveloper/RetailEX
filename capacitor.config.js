/// <reference types="@capacitor/cli" />

const config = {
    appId: 'com.retailex.app',
    appName: 'RetailEX',
    webDir: 'build',
    server: {
        androidScheme: 'https'
    }
};

module.exports = config;

